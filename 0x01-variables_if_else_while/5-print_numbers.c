#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int numbers = 0;
while (numbers >= 9)
{
printf("%d\n", numbers);
numbers++;
}
return (0);
}
